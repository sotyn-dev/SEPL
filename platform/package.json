{
  "name": "@sotyn/platform",
  "private": true,
  "version": "0.1.0",
  "description": "Sotyn control plane — org registry, entitlements, white-label",
  "scripts": {
    "server": "node server/index.js",
    "client": "npm run dev --prefix client",
    "dev": "npx concurrently -n api,ui -c blue,magenta \"npm run server\" \"npm run client\"",
    "agent": "node worker-agent/index.js",
    "gateway": "node gateway-agent/index.js",
    "install:all": "npm install --prefix ./server && npm install --prefix ./client && npm install --prefix ./worker-agent"
  }
}
